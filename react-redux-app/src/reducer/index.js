
//initial state using expected data from API docs
const initialState = {
    adviceData:[]
}
//createing reducer, use state or if none then initialState as default
const reducer = (state = initialState, action) => {

}